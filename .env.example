# Server
PORT=8080
FRONTEND_ORIGIN=http://localhost:3000

# OpenAI
OPENAI_API_KEY=

# =============================================================================
# Supabase Configuration
# =============================================================================
# To find these values:
# 1. Go to https://supabase.com and open your project
# 2. Navigate to: Settings → API (in the left menu)
#
# SUPABASE_URL: Copy from "Project URL" section
# SUPABASE_ANON_KEY: Copy from "anon public" key (safe for client-side)
# SUPABASE_SERVICE_ROLE_KEY: Copy from "service_role" key (server-side ONLY, never expose!)
# =============================================================================
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# =============================================================================
# Stripe Configuration
# =============================================================================
# To find these values:
# 1. Go to https://dashboard.stripe.com and open your project
# 2. Navigate to: Developers → API keys
#
# STRIPE_SECRET_KEY: Copy your secret key (starts with sk_)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: Copy your publishable key (starts with pk_)
#
# For the price IDs:
# 1. Go to Products in Stripe Dashboard
# 2. Create products for single judge ($0.99 AUD one-time) and all judges ($3.99 AUD/month)
# 3. Copy the price IDs (starts with price_)
# =============================================================================
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_PRICE_SINGLE_JUDGE=
STRIPE_PRICE_ALL_JUDGES=
NEXT_PUBLIC_BASE_URL=http://localhost:8080
