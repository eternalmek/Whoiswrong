# Expo app environment variables
# The API base defaults to production if not provided.
EXPO_PUBLIC_API_BASE_URL=https://www.whoiswrong.io
# Optional: connect directly to Supabase if you want client features inside the app
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
